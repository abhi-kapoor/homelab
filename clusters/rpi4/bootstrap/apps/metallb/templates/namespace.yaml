apiVersion: v1
kind: Namespace
metadata:
  name: metallb
  labels:
    # Required for MetalLB speaker - needs NET_ADMIN, NET_RAW, hostNetwork
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/warn: privileged
