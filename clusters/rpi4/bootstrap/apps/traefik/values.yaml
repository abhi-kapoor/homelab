traefik:
  ingressClass:
    enabled: false

  providers:
    kubernetesGateway:
      enabled: true

  gateway:
    enabled: true
    listeners:
      web:
        namespacePolicy: All  # Allow HTTPRoutes from all namespaces
      websecure:
        port: 8443
        protocol: HTTPS
        namespacePolicy: All
